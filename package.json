{
  "name": "viwo",
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "dev": "bun --filter '*' dev",
    "typecheck": "bun --filter '*' typecheck"
  },
  "devDependencies": {
    "@tsconfig/strictest": "^2.0.8",
    "@types/bun": "^1.3.3"
  }
}
